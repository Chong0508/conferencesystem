# Use Railway-provided environment variables
spring.datasource.url=dbc:mysql://localhost:3306/cmsdb
spring.datasource.username=root
spring.datasource.password=CMKsql0508*
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Let Hibernate manage schema only if needed, or keep none if you have SQL scripts
spring.jpa.hibernate.ddl-auto=update  

# Show SQL for debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Railway dynamically assigns a port
server.port=${PORT:8080}

